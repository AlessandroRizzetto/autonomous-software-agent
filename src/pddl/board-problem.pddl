(define (problem board-problem)
    (:domain board-domain)
    (:objects
    )

    (:init
        ;todo: put the initial state's facts and numeric values here
    )

    (:goal
        (and
            ;todo: put the goal condition here
        )
    )
)